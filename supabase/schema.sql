-- DO NOT EXECUTE THIS FILE.
--
-- supabase/schema.sql is a non-executable pointer, not a schema dump.
-- Do not run it in the SQL editor, via psql, or as a migration.
--
-- Canonical schema: supabase/migrations/
-- Apply those files in order (supabase db push / npm run db:ci:local).
-- The latest migration filename is not a frozen contract.
--
-- The historical dump that lived here was removed because executing it
-- destroyed public.tenants and public.tenant_memberships (and other live objects).
-- Git history retains that dump if you need to inspect it.
