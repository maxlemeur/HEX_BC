/**
 * Leaf module: numeric limits shared by validation schemas and domain modules.
 * Import-free on purpose (see `enums.ts`).
 */
export const TAKEOFF_COMPARE_MIN_THRESHOLD = 0.5;
export const TAKEOFF_COMPARE_MAX_THRESHOLD = 0.99;
export const TAKEOFF_DPGF_COMPARE_MAX_PAGE_SIZE = 200;
